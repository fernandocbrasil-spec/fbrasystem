services:
  postgres:
    image: postgres:16-alpine
    container_name: pf-advogados-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: pf_advogados
      POSTGRES_USER: pf
      POSTGRES_PASSWORD: pf2026
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
